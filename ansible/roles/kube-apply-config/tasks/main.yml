- name: Copy kubeconfig to user home
  ansible.builtin.copy:
    src: /root/.kube/config
    dest: "/home/{{ podman_user }}/.kube/config"
    owner: "{{ podman_user }}"
    mode: '0600'
  become: true

