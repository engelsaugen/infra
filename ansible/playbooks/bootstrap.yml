- name: Bootstrap a local node for dev operations
vars:
  podman_user: "podman_user"
  hosts: localhost
  become: true
  roles:
    - common
    - podman
    - kube-tools
    - observability-dirs
    - grafana
    - prometheus
    - loki
